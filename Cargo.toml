[package]
name = "unnamed-cli-project"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
crossterm = "0.20.0"

[dependencies.tui]
version = "0.15.0"
default-features = false
features = ["crossterm"]